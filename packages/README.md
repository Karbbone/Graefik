# packages/

Emplacement des paquets partagés du monorepo (types partagés, config, UI, etc.).

Chaque paquet doit avoir son propre `package.json` avec un nom `@graefik/<nom>`
pour être détecté par les workspaces bun et Turborepo.
