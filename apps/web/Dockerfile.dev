# syntax=docker/dockerfile:1
FROM oven/bun:1

WORKDIR /app

# Les dependances et le code sont montes via bind mount par docker-compose.
# "bun install && bun run dev --host" est lance par la commande du compose.
EXPOSE 5173
CMD ["bun", "run", "dev", "--host"]
